# 2026-09-10 今日内容

## 做了哪些内容

- 安装并验证 LangChain、LangGraph 和 langchain-openai，统一使用 E:\stock-agent\.venv。
- 新增 tools/screening_tools.py 和 tools/market_tools.py，封装量化筛选、评分和数据状态检查。
- 新增 workflow/graph.py，建立“用户请求 → 计划节点 → 量化节点 → 候选结果”流程。
- 新增 agent/quant_agent.py，作为量化 Agent 入口；计算仍由 Python 完成。
- 新增 runtime/model.py，集中配置 DeepSeek 模型和 API 密钥。
- 修改 main.py 和 FastAPI 接口，统一调用新的 LangGraph 主链路。
- 新增 POST /stocks/research、GET /stocks/run、GET /stocks/runs/{run_id}。
- 新增 run_id 和 output/runs/{run_id}.json，避免任务结果互相覆盖。
- 修正数量参数、日期校验、因子缺失值、异常数据和 API 启动问题。

## 为什么这样做

量化计算和评分是确定性任务，交给 Python 更稳定、更容易复现。LangChain 和 LangGraph 放在外层，负责理解请求、管理流程和调用量化工具。统一命令行与 API 的主链路，避免维护多套执行逻辑。

## 达成的效果

当前 MVP 主链路已经跑通：

```text
用户请求 → DeepSeek 解析数量和日期 → LangGraph → 量化 Agent → Python 筛选评分 → 保存并查询结果
```

当前 MVP 包含总 Agent 的基础规划能力和量化 Agent；新闻研究 Agent、复核 Agent、自动交易等内容没有加入。

## 原链路与 LangGraph 主链路的区别

原来的链路是固定脚本顺序：

```text
main.py
→ build_factor.py
→ stock_filter.py
→ stock_selector.py
→ research_agent.py
→ analysis_agent.py
→ stock_analysis.json
```

不管用户提出什么问题，原链路都会按相同顺序执行。用户只想查一只股票时，也会执行整套固定脚本。

现在的 LangGraph 主链路是：

```text
用户请求
→ 计划节点
→ 量化节点
→ 候选结果
```

计划节点负责理解用户请求中的数量和日期。例如：

```text
请按2026年8月20日的数据筛选3只股票
```

会转换成：

```json
{
  "top_n": 3,
  "as_of": "2026-08-20"
}
```

量化节点再调用 Python 量化函数，读取对应日期的因子数据、去除 ST、评分排序并返回 3 只股票。LangGraph 没有重写量化逻辑，而是负责组织节点和传递参数。

它的价值是以后可以根据任务选择不同路径。例如增加研究节点后，用户只要求筛选时可以跳过新闻研究，用户要求事件分析时再进入研究节点。

## run_id 和结果文件

`run_id` 可以理解为每次任务的唯一编号，不是股票代码。

如果每次都写入同一个文件：

```text
output/stock_analysis.json
```

后一次运行会覆盖前一次，无法区分不同任务的结果。

现在每次运行会生成一个编号，例如：

```text
20260910000613-896404
```

结果保存为：

```text
output/runs/20260910000613-896404.json
```

下一次运行会使用另一个编号和文件，因此不会覆盖上一次结果：

```text
output/runs/20260910000725-a81f20.json
```

创建任务时，接口返回 `run_id`：

```json
{
  "run_id": "20260910000613-896404",
  "status": "completed"
}
```

之后通过下面的接口查询这一轮结果：

```text
GET /stocks/runs/20260910000613-896404
```

这样可以准确区分不同请求、重新读取历史结果，并避免任务之间互相覆盖文件。

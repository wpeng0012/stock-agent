# V0.3 新闻和公告研究

发布日期：2026-09-14

## 版本定位

V0.3在V0.2确定性量化筛选和个股查询基础上，增加按需新闻公告研究。系统只研究指定股票或筛选后的少量候选，不建设全市场PDF仓库，不改变原量化评分。

## 新增功能

- 按股票和日期查询巨潮公告、解禁事件、深市互动易、东方财富个股新闻及Tavily补充新闻。
- 新增 `stock-event-research` Skill，约束证据等级、研究范围和影响表述。
- 自动识别业绩、解禁、定增、回购、增减持、分红、合同、重组、股权激励、诉讼监管、担保质押及风险提示等事件。
- 为研究材料生成 `E001` 格式的证据编号，并合并重复链接。
- 对少量重大公告解析巨潮原始PDF；临时文件处理后自动删除，结果只保留必要证据片段。
- DeepSeek输出重要事件、正面因素、风险因素、影响期限、不确定性和数据缺口；所有事件判断必须引用有效证据编号。
- 新增个股新闻公告研究和“筛选后研究前3只”两条LangGraph路径。
- 新增 `POST /stocks/event-research`，结果使用原有 `run_id` 保存和查询。
- 第三方数据源运行在隔离进程中，具有硬超时、最大记录数、90天时间范围、轻量缓存和2GB缓存上限。
- 单个来源或模型失败时返回 `partial`；没有证据时返回 `no_data`，不伪造完整结果。

## 数据源与边界

- 不使用Tushare付费接口。
- V0.3不包含股吧、雪球等社区情绪。
- 当前上证e互动接口会全量翻页，因此沪市董秘问答暂时返回来源不可用，不影响其他来源。
- 解禁不代表实际减持，回购计划不代表完成回购，定增预案不代表发行完成。
- 研究结果用于信息整理和研究辅助，不构成投资建议，不执行自动交易。

## 使用方式

API：

```text
POST /stocks/event-research
```

请求示例：

```json
{
  "stock": "000001",
  "start_date": "2026-08-01",
  "end_date": "2026-09-14",
  "use_model": true
}
```

本地检查：

```powershell
.\.venv\Scripts\python.exe src\check_event_research.py 000001 --stock-name 平安银行 --start-date 2026-08-01 --end-date 2026-09-14
```
